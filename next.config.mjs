/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com'], // Add your external image hostnames here
        domains: ['images.unsplash.com'],
      },
};

export default nextConfig;
